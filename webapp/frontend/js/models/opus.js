var app = app || {};

(function() {
	'use strict';

	app.Opus = Backbone.Model.extend({

		/* id: ObjectId
		 * title: String
		 * year: Number
		 * author: ObjectId
		 * original: ObjectId
		 * translator: ObjectId
		 */

	});

}());
