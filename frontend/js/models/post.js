var app = app || {};

(function() {
	'use strict';

	app.Post = Backbone.Model.extend({

		/* id: ObjectId
		 * user: ObjectId
		 * date: Date
		 * opus: ObjectId
		 * summary: String
		 * tags: [String]
		 */

	});

}());
